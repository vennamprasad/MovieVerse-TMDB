<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MovieVerse.Starting" parent="Theme.SplashScreen">
        <item name="android:windowBackground">@color/splash_screen_background</item>
        <item name="windowSplashScreenBackground">@color/splash_screen_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/movieverse</item>
        <item name="postSplashScreenTheme">@style/Theme.MovieVerse</item>
    </style>

    <style name="Theme.MovieVerse" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

</resources>